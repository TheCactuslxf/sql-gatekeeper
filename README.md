# SQL Gatekeeper

Python implementation of a SQL gatekeeper service for LLM-generated MySQL SQL.

## Goals

- Accept SQL generated by an LLM.
- Route logical tables to physical tables across multiple MySQL instances.
- Rewrite SQL when the input only contains logical table names.
- Run automatic safety checks before execution.
- Reject high-risk SQL without human approval.
- Execute approved SQL with read-only credentials.

## Constraints

- Language: Python
- API style: service/API form
- Database target: MySQL
- Local development database: Docker MySQL
- Every coding task must include unit tests
- Safety checks are performed on rewritten physical SQL

## High-Level Modules

- API layer
- SQL parser
- Route decision engine
- SQL rewrite engine
- Security filter chain
- Metadata / EXPLAIN risk evaluator
- Executor
- Audit logger

## Business Shard Factor Contract

For imported sharded tables, callers can provide the business shard column name while keeping
the shard value in the logical SQL predicate:

```json
{
  "sql": "select count(1) as cnt from partner__partner_relation_info where from_uid = '97585024' limit 1",
  "route_context": {"shard_column": "from_uid"}
}
```

The service extracts the predicate value, computes the route from imported shard metadata,
rewrites the logical table to the physical table, and executes the checked SQL. Callers do not
need to calculate or provide `route_suffix`.

Existing imported metadata remains compatible through the runtime fallback. To regenerate the
metadata with the new business-factor route definitions, run:

```bash
PYTHONPATH=src .venv/bin/python -m sql_gatekeeper.tools.spy_table_config_sync
```

The sync command requires the existing `SPY_DB_*` environment variables, including
`SPY_DB_PASSWORD`.

## Initial Development Order

1. Project skeleton and local Docker environment
2. Metadata schema and repositories
3. API contracts
4. SQL parsing and route decision
5. SQL rewrite
6. Filter chain and first security filters
7. Metadata loading and EXPLAIN-based risk checks
8. Execution, audit, and end-to-end tests

## Repo Conventions

- Keep key project decisions in `docs/context.md`
- Put application code under `src/sql_gatekeeper`
- Put tests under `tests`
- Prefer small, composable modules
- Add tests together with each code increment
